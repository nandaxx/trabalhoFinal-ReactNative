import React, { createContext, useState } from "react";

export const DetalhesContext = createContext({});

export const DetalhesProvider = ({ children }: any) => {

    

    return (
        
    )
};